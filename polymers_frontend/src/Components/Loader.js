import React from 'react';
import './Loader.css'; // Ensure this CSS file is in the same folder or adjust the path accordingly

const Loader = () => {
  return (
    <div className="loader"></div>
  );
};

export default Loader;
